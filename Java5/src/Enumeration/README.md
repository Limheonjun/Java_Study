# Enumeration

## Decription
- 관련있는 상수들의 집합

## Features
- 열거체의 첫번째 상수값은 0부터 설정
- 불규칙한 상수값으로 설정시엔 상수의 이름옆에 괄호를 추가, 그 안에 원하는 값을 명시할 수 있음. 이때는 변수의 생성자를 별도로 추가해야함
- values() : 해당 열거체의 모든 상수를 저장한 배열을 반환
- valuesOf(상수) : 상수값과 일치하는 열거체의 상수 반환